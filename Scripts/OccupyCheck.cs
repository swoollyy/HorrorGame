using UnityEngine;

public class OccupyCheck : MonoBehaviour
{
    public bool isOccupied;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        isOccupied = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
